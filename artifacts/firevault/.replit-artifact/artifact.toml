kind = "web"
previewPath = "/"
title = "FPS Brandpreventie"
version = "1.0.0"
id = "artifacts/firevault"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25392

[services.development]
run = "pnpm --filter @workspace/firevault run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/firevault", "run", "build" ]
publicDir = "artifacts/firevault/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25392"
BASE_PATH = "/"
